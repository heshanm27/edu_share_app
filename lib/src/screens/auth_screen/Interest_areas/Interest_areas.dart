import 'package:flutter/cupertino.dart';

class InterestAreas extends StatelessWidget {
  const InterestAreas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
